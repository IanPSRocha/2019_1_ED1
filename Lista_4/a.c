#include <stdio.h>
#include <stdlib.h>

int main(){
    int n;
    scanf("%d", &n);

    int v[n];
    for(int i = 0; i < n; i++){ // Preenchendo o vetor de notas
        scanf("%d", &v[i]);
    }

    int aux[202];
    for(int i = 0; i < 202; i++){
        aux[i] = 0;
    }

    for(int i = 0; i < n; i++){
        
    }

return 0;
}